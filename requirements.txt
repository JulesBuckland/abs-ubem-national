# Runtime dependencies, verified against the project's imports by AST scan.
#
# Version floors are the versions the published results were produced with
# (Python 3.12.3). For a byte-identical environment install
# requirements-lock.txt instead, which pins the full transitive tree.
#
# joblib, shapely and pyarrow are listed explicitly even though scikit-learn,
# geopandas and pandas would pull them in: joblib and shapely are imported
# directly, and pyarrow is the parquet engine the pipeline's .parquet I/O
# depends on. All three were previously undeclared, so a clean install could
# not run the pipeline.

# Numerics and data
numpy>=2.4.6
pandas>=3.0.3
scipy>=1.18.0
pyarrow>=25.0.0
joblib>=1.5.3

# Bayesian inference
pymc>=6.1.0
pytensor>=3.1.3
arviz>=1.2.0

# Spatial
geopandas>=1.1.4
shapely>=2.1.2
libpysal>=4.15.0

# Surrogate model
scikit-learn>=1.9.0

# Validation, plotting, CLI and progress reporting
pandera>=0.32.1
matplotlib>=3.11.0
rich>=15.0.0
tqdm>=4.68.4
colorama>=0.4.6
psutil>=7.2.2
